
create a hive script like this e.g. hive_script.sql and call it by using the below command alongwith parms.

set hive.variable.substitute=true;
set DEPT=${hiveconf:DEPT};
use default;
select t.*,t.dept1 fromm ( select *, case when dept='cse' then 'cse' else 'hi' end as dept1 from result)t
where t.dept1='${DEPT}'

use acid_db;

insert into hello_acid partition(load_date='${hiveconf:insert_date}') values (7,7);

#hive -v --hiveconf DEPT='cse' --hiveconf insert_date=2018-11-13 -f hive_script.hql